/* return Promise.race([connect(peripheral), timeout("connect", ms)]);
return Promise.race([disconnect(peripheral), timeout("disconnect", ms)]);

 */
/* const [service] = await Promise.race([
      discoverServices(peripheral, [uuid]),
      timeout("disconnect", ms)
    ]); */

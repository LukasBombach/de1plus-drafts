type Query {
  connected: Boolean
}

type Mutation {
  connect: ConnetionEvent
  disconnect: ConnetionEvent
}

enum ConnetionEvent {
  CONNECTED
  DISCONNECTED
  ALREADY_CONNECTED
  ALREADY_DISCONNECTED
}

type Query {
  connected: Boolean
}

type Mutation {
  connect: ConnetionEvent
  disconnect: ConnetionEvent
  power(state: Boolean!): Boolean
}

enum ConnetionEvent {
  CONNECTED
  DISCONNECTED
  ALREADY_CONNECTED
  ALREADY_DISCONNECTED
}
